name: שורה_כּלי
targets:
  שורה_כּלי:
    type: tool
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: Sources
        buildPhase: sources
    dependencies:
      - package: גיך
        product: ביבליאָטעק
    settings:
      base:
        SWIFT_VERSION: "6.0"
packages:
  גיך:
    path: ../..
